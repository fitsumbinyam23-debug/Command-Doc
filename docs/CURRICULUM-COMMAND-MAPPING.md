# Curriculum Command Mapping

Command placement is curated in `data/curriculum/curriculum-command-placement.json`; the generator validates and enriches it into `data/curriculum/curriculum-command-map.json`.
